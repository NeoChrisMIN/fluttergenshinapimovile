import 'package:flutter/material.dart';

class ArtifactPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Artifacts Page (en construcción)',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
